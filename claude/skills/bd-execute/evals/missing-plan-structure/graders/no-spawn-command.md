---
type: tool_used
tool: Bash
input_match: 'spawn-crewmate\.sh'
max: 0
---

Mechanical check: spawn-crewmate.sh must never be executed — this plan lacks the structure
bd-execute requires, so the run must stop before any spawning.
